import React from 'react';

const Header = () => {
    return (
        <div>
            <h1>Header</h1>
        </div>
    );
};

const anotherHeader = () => {
    return (
        console.log("I'm doing something")
    );
};

export {anotherHeader};

export default Header;